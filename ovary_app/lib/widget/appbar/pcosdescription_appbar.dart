import 'package:flutter/material.dart';

class PcosDescriptionAppbar extends StatelessWidget {
  const PcosDescriptionAppbar({super.key});

  @override
  Widget build(BuildContext context) {
    return AppBar(
      title: const Text("PCOS란?"),
    );
  }
}