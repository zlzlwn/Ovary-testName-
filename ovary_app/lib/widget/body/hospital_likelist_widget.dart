import 'package:flutter/material.dart';

class HospitalLikeListWidget extends StatelessWidget {
  const HospitalLikeListWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("찜목록"),
    );
  }
}