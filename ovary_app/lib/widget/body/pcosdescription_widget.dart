import 'package:flutter/material.dart';

class PcosDescriptionWidget extends StatelessWidget {
  const PcosDescriptionWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("pcosdescription"),
    );
  }
}