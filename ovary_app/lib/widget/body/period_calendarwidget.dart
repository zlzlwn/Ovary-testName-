import 'package:flutter/material.dart';

class PeriodCalenderWidget extends StatelessWidget {
  const PeriodCalenderWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return  Center(
        child: Text("period_calendar"),
      );
    
  }
}