import 'package:flutter/material.dart';

class WeightChangeChart extends StatelessWidget {
  const WeightChangeChart({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}